---

-  name: update node count
   replace:
     path: "{{ lookup('env', 'WORKSPACE') }}/environments/{{ master_project_name }}/all"
     replace: "{{ replace }}"
     regexp: '(?<=f.\n)(\s*\w*:\s*).'
---
- name: Write existing vnet prefixes to file
  lineinfile: 
    line: "{{ vnet_prefix }}" 
    path: roles/calculate-vnet-prefix/files/prefixfile
    create: yes
  loop: "{{ remote_address_space.address_prefixes|flatten(levels=1) }}"
  loop_control:
    loop_var: vnet_prefix        
